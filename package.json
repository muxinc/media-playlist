{
  "name": "media-playlist",
  "version": "0.1.0",
  "description": "Play through a set of video and audio elements.",
  "type": "module",
  "main": "dist/media-playlist.js",
  "files": [
    "./dist/*",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf dist && rollup --config",
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish-patch": "yarn run build && np patch --no-tests --any-branch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/muxinc/media-playlist.git"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "keywords": [
    "playlist",
    "media",
    "player",
    "audio",
    "video"
  ],
  "author": "@heff",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@rollup/plugin-commonjs": "^11.0.2",
    "@rollup/plugin-node-resolve": "^7.1.1",
    "np": "^6.2.0",
    "rollup": "^2.2.0",
    "rollup-plugin-babel-minify": "^10.0.0",
    "rollup-plugin-terser": "^5.3.0"
  }
}
